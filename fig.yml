proxy:
  image: nginx:1.7.6
  links:
    - registry
  volumes:
    - proxy.conf:/etc/nginx/nginx.conf
    - proxy.htpasswd:/etc/nginx/htpasswd
  ports:
    - "8888:80"

registry:
  image: registry:0.8.1
  volumes:
    - registry-config.yml:/docker-registry/config/config_sample.yml
